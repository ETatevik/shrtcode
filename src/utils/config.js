export const translations = {
    PLACEHOLDER_INPUT: "Input the ULR",
    SUBMIT_BUTTON: "SHORTEN",
    SHORT_LINK: "Short URL",
    SHARE_LINK: "Share URL",
    ORIGIN_LINK: "Original URL",
    EMPTY_DATA: "There isn't any data",
    RESULT: "RESULT"
}

export const uniqueKey = () => {
    return Math.round(Math.random() * 10000);
}